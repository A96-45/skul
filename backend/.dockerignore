# Dependencies (will be installed during build)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
bun.lock

# Environment files (should be mounted or passed as env vars)
.env
.env.local
.env.development
.env.production

# Build outputs (not needed for container builds)
dist/
build/

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Git (not needed in container)
.git/
.gitignore

# Docker files (not needed in container)
Dockerfile*
.dockerignore

# Logs (will be created at runtime)
logs/
*.log

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Coverage directory
coverage/
.nyc_output/

# Dependency cache
.npm/
.yarn/

# Development files
README.md
DEVELOPER_QUICK_START.md

# Test files (not needed in production)
__tests__/
**/*.test.js
**/*.test.ts
**/*.spec.js
**/*.spec.ts
test-*.js

# Source maps (not needed in production)
**/*.map

# Temporary files
tmp/
temp/

# Cache directories
.cache/

# Database files (should use volumes)
*.db
*.sqlite
*.sqlite3

# Upload files (should use volumes)
uploads/
